import React,{useEffect} from 'react'

export default function CommonTitle({title }) {
    useEffect(() => {
        return () => {
        }
    }, [])
    return (
        <div>
            <p>{title}</p>
        </div>
    )
}

